version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: treecare_postgres
    environment:
      POSTGRES_USER: treecare
      POSTGRES_PASSWORD: treecare123
      POSTGRES_DB: treecare_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U treecare"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - treecare_network

  redis:
    image: redis:7-alpine
    container_name: treecare_redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - treecare_network

volumes:
  postgres_data:

networks:
  treecare_network:
    driver: bridge